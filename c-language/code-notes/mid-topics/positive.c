#include <stdio.h>
int main()
{
    int number;
    scanf("%d",&number);
    if(number>0)printf("N is positive");
    else if(number<0)printf("N is negative");
    else print("N is 0");
    return 0;
} 